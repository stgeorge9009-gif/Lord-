# Auto detect text files and perform LF normalization
* text=auto eol=lf

# Explicitly mark binary files so Git never modifies them during checkout/commit
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.keystore binary
gradle/wrapper/gradle-wrapper.jar binary
